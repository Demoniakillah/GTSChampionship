{% extends 'base.html.twig' %}

{% block title %}Race Configuration{% endblock %}

{% block body %}
    <h1>Race Configuration</h1>

    <table class="table table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ race_configuration.id }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ race_configuration.value }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('race_configuration_index') }}">back to list</a>

    <a href="{{ path('race_configuration_edit', {'id': race_configuration.id}) }}">edit</a>

    {{ include('race_configuration/_delete_form.html.twig') }}
{% endblock %}
