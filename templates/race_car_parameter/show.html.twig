{% extends 'base.html.twig' %}

{% block title %}RaceCarParameter{% endblock %}

{% block body %}
    <h1>RaceCarParameter</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ race_car_parameter.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ race_car_parameter.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('race_car_parameter_index') }}">back to list</a>

    <a href="{{ path('race_car_parameter_edit', {'id': race_car_parameter.id}) }}">edit</a>

    {{ include('race_car_parameter/_delete_form.html.twig') }}
{% endblock %}
