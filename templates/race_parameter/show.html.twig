{% extends 'base.html.twig' %}

{% block title %}RaceParameter{% endblock %}

{% block body %}
    <h1>RaceParameter</h1>

    <table class="table table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ race_parameter.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ race_parameter.name }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ race_parameter.type }}</td>
            </tr>
            <tr>
                <th>AvailableValues</th>
                <td>{{ race_parameter.availableValues }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('race_parameter_index') }}">back to list</a>

    <a href="{{ path('race_parameter_edit', {'id': race_parameter.id}) }}">edit</a>

    {{ include('race_parameter/_delete_form.html.twig') }}
{% endblock %}
