<form method="post" action="{{ path('pool_configuration_delete', {'id': pool_configuration.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pool_configuration.id) }}">
    <button class="btn">Delete</button>
</form>
