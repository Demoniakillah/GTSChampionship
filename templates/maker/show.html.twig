{% extends 'base.html.twig' %}

{% block title %}Maker{% endblock %}

{% block body %}
    <h1>Maker</h1>

    <table class="table table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ maker.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ maker.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('maker_index') }}">back to list</a>

    <a href="{{ path('maker_edit', {'id': maker.id}) }}">edit</a>

    {{ include('maker/_delete_form.html.twig') }}
{% endblock %}
