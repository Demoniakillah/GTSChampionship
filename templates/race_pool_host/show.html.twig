{% extends 'base.html.twig' %}

{% block title %}RacePoolHost{% endblock %}

{% block body %}
    <h1>RacePoolHost</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ race_pool_host.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ race_pool_host.value }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('race_pool_host_index') }}">back to list</a>

    <a href="{{ path('race_pool_host_edit', {'id': race_pool_host.id}) }}">edit</a>

    {{ include('race_pool_host/_delete_form.html.twig') }}
{% endblock %}
